# Copy to ``.env`` and set values. Flask loads ``.env`` on startup.

# Base URL of the Flask API (used by admin dashboard and mirrored to assets/dashboard_config.json)
LUXSCALE_DASHBOARD_API_BASE=http://127.0.0.1:5000
